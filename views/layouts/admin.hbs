<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{title}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />

  <!-- Styles -->
  <style>
  body { margin: 0; font-family: "Inter", sans-serif; background-color: #f9f9fb; }
  .admin-wrapper { min-height: 100vh; overflow: hidden; }
  .admin-sidebar {
    width: 230px;
    background: #dc3545;
    color: white;
    min-height: 100vh;
    position: fixed;
    top: 0; left: 0;
  }
  .admin-main { margin-left: 230px; overflow-y: auto; }
  .nav-link {
    color: rgba(255,255,255,0.85);
    font-weight: 500;
    border-radius: 8px;
    padding: 10px 15px;
    transition: all 0.2s ease;
  }
  .nav-link:hover, .nav-link.active {
    background-color: rgba(255,255,255,0.2);
    color: #fff;
    text-decoration: none;
  }
  @media (max-width: 768px) {
    .admin-sidebar { position: static; width: 100%; height: auto; }
    .admin-main { margin-left: 0; }
  }
  </style>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
  <div class="admin-wrapper d-flex">
    <!-- Sidebar -->
    <nav class="admin-sidebar d-flex flex-column justify-content-between p-3">
      <div>
        <h4 class="text-white mb-4 text-center fw-bold">✈️ Flight Admin</h4>
        <ul class="nav flex-column">
          <li class="nav-item mb-2">
            <a href="/admin" class="nav-link text-white {{#if (eq title 'Admin Dashboard')}}active{{/if}}">
              <i class="bi bi-speedometer2 me-2"></i> Dashboard
            </a>
          </li>
          <li class="nav-item mb-2">
            <a href="/admin/flights" class="nav-link text-white {{#if (eq title 'All Flights')}}active{{/if}}">
              <i class="bi bi-airplane me-2"></i> Flights
            </a>
          </li>
          <li class="nav-item mb-2">
            <a href="/admin/create" class="nav-link text-white {{#if (eq title 'Create Flight')}}active{{/if}}">
              <i class="bi bi-plus-circle me-2"></i> Create Flight
            </a>
          </li>
          <li class="nav-item mb-2">
            <a href="/admin/reservations" class="nav-link text-white {{#if (eq title 'Reservations')}}active{{/if}}">
              <i class="bi bi-journal-bookmark me-2"></i> Reservations
            </a>
          </li>
        </ul>
      </div>
      <div>
        <a href="/users/logout" class="btn btn-outline-light w-100 mt-3">
          <i class="bi bi-box-arrow-right me-2"></i> Logout
        </a>
      </div>
    </nav>

    <!-- Main Content -->
    <main class="admin-main">
      {{{body}}}
    </main>
  </div>

  
</body>
</html>
