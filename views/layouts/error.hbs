<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

    <style>
        body {
            background: #fafafa;
        }
        .error-card {
            max-width: 500px;
            border-radius: 15px;
            padding: 30px;
        }
        .error-code {
            font-size: 8rem;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div class="d-flex justify-content-center align-items-center vh-100">
        <div class="text-center shadow error-card bg-white">
            
            <h1 class="error-code text-danger">{{code}}</h1>
            <h2>{{message}}</h2>
            <p>{{description}}</p>

            {{#if error}}
                <p class="text-muted"><small>Error: {{error}}</small></p>
            {{/if}}

            {{#if errorId}}
                <p class="text-muted"><small>Error ID: {{errorId}}</small></p>
            {{/if}}

            <a href="/" class="btn btn-danger mt-2">Go Home</a>
        </div>
    </div>
</body>
</html>
