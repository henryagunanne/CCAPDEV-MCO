<div class="container text-center my-5">
  <h2 class="text-success fw-bold mb-4">Booking Confirmed!</h2>
  <p>Thank you, <strong>{{reservation.fullName}}</strong>.</p>
  <p>Your flight from <strong>{{reservation.flight.origin}}</strong> 
     to <strong>{{reservation.flight.destination}}</strong> is confirmed.</p>

  <div class="card shadow-sm mt-4 p-4 mx-auto" style="max-width: 600px;">
    <h4 class="text-danger mb-3">Flight Summary</h4>
    <p><strong>Flight No:</strong> {{reservation.flight.flightNumber}}</p>
    <p><strong>Departure:</strong> {{reservation.flight.departureTime}}</p>
    <p><strong>Arrival:</strong> {{reservation.flight.arrivalTime}}</p>
    <p><strong>Class:</strong> {{reservation.travelClass}}</p>
    <hr>
    <p><strong>Seat:</strong> {{reservation.seatNumber}}</p>
    <p><strong>Meal:</strong> {{reservation.meal}}</p>
    <p><strong>Baggage:</strong> {{reservation.baggageAllowance}} kg</p>
    <p><strong>Status:</strong> {{reservation.status}}</p>
  </div>

  <div class="mt-4">
    <a href="/" class="btn btn-primary mx-2">Return to Home</a>
    <a href="/reservations/myBookings" class="btn btn-outline-secondary mx-2">View My Reservations</a>
    <a href="/reservations/book/{{reservation.flight._id}}" class="btn btn-danger mx-2">Add Another Passenger</a>
  </div>
</div>
